[package]
name = "tokioutil"
version = "0.1.0"
edition = "2021"

[features]
tokio_tracing = ["tokio/tracing"]

[dependencies]
tokio = "1.43.1"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(tokio_unstable)'] }
